/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  6
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //
//scale 0.001;

actions
(
   // Create set for part 1 - faces below certain z-value
    {
        name    rotorHubRotating;
        type    faceSet;
        action  new;
        source  patchToFace;
        sourceInfo
        {
            patch hub;
        }
    }
    {
        name    rotorHubRotating;
        type    faceSet;
        action  subset;
        source  boxToFace;
        sourceInfo
        {
            box (0 -100 -0.175)(100 100 0.0212);  // Adjust coordinates
        }
    }
    
    // Create set for part 2 - faces above certain z-value
    {
        name    rotorHubStationary1;
        type    faceSet;
        action  new;
        source  patchToFace;
        sourceInfo
        {
            patch hub; // The patch to be splitted 
        }
    }
    {
        name    rotorHubStationary1;   
        type    faceSet;
        action  subset;
        source  boxToFace;
        sourceInfo
        {
            box (0 -100 0.0212)(100 100 0.395);  // Adjust coordinates
        }
    }  
    {
        name    rotorHubStationary0;
        type    faceSet;
        action  new;
        source  patchToFace;
        sourceInfo
        {
            patch hub; // The patch to be splitted 
        }
    }
    {
        name    rotorHubStationary0;   
        type    faceSet;
        action  subset;
        source  boxToFace;
        sourceInfo
        {
            box (0 -100 -0.575)(100 100 -0.175);  // Adjust coordinates
        }
    } 
    {
        name    rotorHubStationary;
        type    faceSet;
        action  new;
        source  faceToFace;
        sourceInfo
        {
           set rotorHubStationary1; // Basically remove this from the original, that way there are no overlaps
        }
     }    
    {
        name    rotorHubStationary;
        type    faceSet;
        action  add;
        source  faceToFace;
        sourceInfo
        {
           set  rotorHubStationary0 ; // Basically remove this from the original, that way there are no overlaps
        }
     }
   // Create set for part 1 - faces below certain z-value
    {
        name    FarDwStrmHub;
        type    faceSet;
        action  new;
        source  patchToFace;
        sourceInfo
        {
            patch hub;
        }
    }
    {
        name   FarDwStrmHub;
        type    faceSet;
        action  subset;
        source  boxToFace;
        sourceInfo
        {
            box (0 -100 0.395)(100 100 0.99);  // Adjust coordinates
        }
    } 
    {
        name    FarDwStrmCas;
        type    faceSet;
        action  new;
        source  patchToFace;
        sourceInfo
        {
            patch casing;
        }
    }
    {
        name   FarDwStrmCas;
        type    faceSet;
        action  subset;
        source  boxToFace;
        sourceInfo
        {
            box (0 -100 0.395)(100 100 0.99);  // Adjust coordinates
        }
    }       
);

// ************************************************************************* //
